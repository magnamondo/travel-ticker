<!--
	Never rendered: the load function above always redirects to the canonical
	/t/<slug>/entry/<id> URL or throws 404. SvelteKit still needs a component
	for the route to exist.
-->
<p>Redirecting…</p>
